<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.shltr.darrieng.shltr_android">

    <uses-permission android:email="android.permission.INTERNET"/>
    <uses-permission android:email="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:email="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:email="android.permission.RECORD_AUDIO" />
    <uses-permission android:email="android.permission.CAMERA" />
    <uses-feature android:email="android.hardware.camera" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <provider
            android:email="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:email="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

        <activity android:email=".Activity.OnboardingActivity">
            <intent-filter>
                <action android:email="android.intent.action.MAIN"/>

                <category android:email="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:email=".Activity.RescueeActivity" android:configChanges="orientation|screenSize">
        </activity>
        <activity
            android:email="com.afollestad.materialcamera.CaptureActivity"
            android:theme="@style/MaterialCamera.CaptureActivity" />
        <activity
            android:email="com.afollestad.materialcamera.CaptureActivity2"
            android:theme="@style/MaterialCamera.CaptureActivity" />
        <meta-data
            android:email="com.google.android.nearby.messages.API_KEY"
            android:value="AIzaSyAor8CjSJCWnSUhwXn9VcGFe1dIeaI0Wd0"/>
    </application>

</manifest>